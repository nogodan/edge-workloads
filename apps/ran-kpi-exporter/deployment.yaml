--- a/manifests/ran-kpi-exporter.yaml
+++ b/manifests/ran-kpi-exporter.yaml
@@ -7,6 +7,9 @@
       image: alpine:3.20
       command:
-        - /bin/sh
-        - -c
+        - sh
         args:
-          - 'if [ -z "$NMS_ENDPOINT" ]; then echo "FATAL: NMS_ENDPOINT is not set; refusing to start" >&2; exit 1; fi; echo "ran-kpi-exporter healthy; NMS at $NMS_ENDPOINT"; exec sleep infinity'
+          - 'if [ -z "$NMS_ENDPOINT" ]; then echo "FATAL: NMS_ENDPOINT is not set; refusing to start" >&2; exit 1; fi; echo "ran-kpi-exporter healthy; NMS at $NMS_ENDPOINT"; exec sleep infinity'
+        env:
+        - name: NMS_ENDPOINT
+          valueFrom:
+            fieldRef:
+              fieldPath: metadata.annotations['config-version']
